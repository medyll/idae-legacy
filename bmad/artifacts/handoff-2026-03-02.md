# BMAD Handoff — 2026-03-02

> Written for agent continuity. The next agent must read this before executing `/next`.

---

## Current State

| Key | Value |
|-----|-------|
| Sprint | **02 — ACTIVE** (S2-01 through S2-05, all pending) |
| Last completed | S1-09 — core SCSS files created (`_vars.scss`, `_app_fabric.scss`, `_style.scss`) |
| In-progress | **S2-01** — analysis done, no files written yet |
| Recommendation | `/dev-story S2-01` |

---

## Sprint 02 Stories

| ID | Title | Pts | Status |
|----|-------|-----|--------|
| S2-01 | Convert remaining 26 `.less` → `.scss` | 5 | 🔄 analysis done, **start here** |
| S2-02 | Replace `@{location_fragment}` with explicit `@use` in `main.scss` | 2 | ⏳ pending |
| S2-03 | Convert remaining LESS mixins → `@mixin`/`@include` | 2 | ⏳ pending |
| S2-04 | Remove `lessc.inc.php` from `index.php` boot path | 1 | ⏳ pending |
| S2-05 | `ClassApp.__construct` modernization | 3 | ⚠️ likely already done — verify before coding |

---

## S2-01 — File Analysis (already complete, use this directly)

Target directory: `idae/web/appcss/less/`  
Output directory: `idae/web/appcss/scss/`  
Already converted (Sprint 01): `css/_vars.scss`, `css/_app_fabric.scss`, `css/_style.scss`

### Group A — Pure CSS: just copy + rename to `.scss` (no conversion needed)

| Source `.less` | Target `.scss` |
|----------------|----------------|
| `vendor/animate-nc.less` | `vendor/_animate-nc.scss` |
| `vendor/swiper/core.less` | `vendor/swiper/_core.scss` |
| `vendor/swiper/effects.less` | `vendor/swiper/_effects.scss` |
| `vendor/swiper/navigation.less` | `vendor/swiper/_navigation.scss` |
| `vendor/swiper/navigation-f7.less` | `vendor/swiper/_navigation-f7.scss` |
| `vendor/swiper/preloader.less` | `vendor/swiper/_preloader.scss` |
| `vendor/swiper/preloader-f7.less` | `vendor/swiper/_preloader-f7.scss` |
| `vendor/swiper/scrollbar.less` | `vendor/swiper/_scrollbar.scss` |
| `vendor/swiper/swiper.less` | `vendor/swiper/_swiper.scss` |
| `vendor/swiper/zoom.less` | `vendor/swiper/_zoom.scss` |
| `sortable.less` | `_sortable.scss` |
| `swiper-override.less` | `_swiper-override.scss` |

### Group B — LESS variable usages (`@var` → `$var`) + `@use "vars" as *` header

These files USE variables from `vars.less` but don't define their own. They need the `@use` forward + `@var` → `$var` in property values.

| Source | Target | Notes |
|--------|--------|-------|
| `css/global-tokens.less` | `css/_global-tokens.scss` | Uses `@font_inter`, `@font-size-base`, `@color-text`, `@btn-*`, `@input-*`, `@heading-weight` |
| `pikaday.less` | `_pikaday.scss` | Uses `@font_inter` only |

### Group C — LESS imports + mixin calls (need `@use` + `@include` conversion)

These files import vars/app_fabric and call mixins as `.mixin` (LESS style).

| Source | Target | Mixin calls to convert |
|--------|--------|----------------------|
| `css/table.less` | `css/_table.scss` | None detected — just fix `@import "vars/app_fabric"` → `@use` |
| `css/flex_old.less` | `css/_flex-old.scss` | None detected — just fix imports |
| `interface.less` | `_interface.scss` | `.rgba_link` is a class not a mixin call — just fix imports |
| `notifier.less` | `_notifier.scss` | `.animated`, `.slideInRight`, `.speed` are class applications, not mixin calls |

### Group D — Own LESS variables + `@var` → `$var`

These files define their own `@color_N` or similar local vars.

| Source | Target | Vars to convert |
|--------|--------|----------------|
| `skin/skin_default.less` | `skin/_skin-default.scss` | `@color_1..@color_12`, `@font-family_1` |
| `skin/skin_seven.less` | `skin/_skin-seven.scss` | `@color_1..@color_4` |
| `app_window_gui/app_window_gui.less` | `app_window_gui/_app-window-gui.scss` | `@color0..@color1` (+ `.animated`, `.zoomIn`, `.speed` class refs) |

### Group E — LESS loops/mixins (require non-trivial conversion)

| Source | Target | Issue |
|--------|--------|-------|
| `css/flex.less` | `css/_flex.scss` | Uses LESS recursive loops (`.generate-flex_shrink_grow(@n, @i)`) — convert to SCSS `@for` loops |
| `vendor/swiper/mixins.less` | `vendor/swiper/_mixins.scss` | LESS mixin definitions (`.preserve3d()`, `.encoded-svg-background()`) — convert to `@mixin` |

### Group F — Entry points with `@{location_fragment}` (S2-02 handles these)

Do NOT convert these in S2-01 — they are S2-02 scope.

| Source | Target |
|--------|--------|
| `main.less` | `main.scss` (already exists as entry point, needs full `@use` wiring) |
| `appsite.less` | `_appsite.scss` |
| `datatable.less` | `_datatable.scss` |

---

## S2-04 — Quick win (1 line fix)

File: `idae/web/index.php` line containing:
```php
<?  include_once('lessc.inc.php');  ?>
```
→ Remove or comment out this line. The SCSS pipeline replaces the LESS compiler entirely.

---

## S2-05 — Verify before coding

`ClassApp::getMongoClient()` in `idae/web/appclasses/appcommon/ClassApp.php` already uses `MongoDB\Client` with a full singleton pattern, `typeMap` arrays, and `MONGO_HOST` env var. Check lines 139–182.  
**If already done → mark S2-05 done immediately and move on.**

---

## Key Paths

| Path | Purpose |
|------|---------|
| `idae/web/appcss/less/` | LESS source files |
| `idae/web/appcss/scss/` | SCSS output (already has `_vars`, `_app_fabric`, `_style`, `main.scss`) |
| `idae/web/appcss/dist/` | Compiled CSS output |
| `idae/web/app_node/` | `npm run build:css` / `npm run watch:css` |
| `idae/web/appclasses/appcommon/ClassApp.php` | Target for S2-05 verification |
| `idae/web/index.php` | Target for S2-04 (remove lessc.inc.php) |

---

## Variables Reference (for Group B/D conversions)

```
@border_color   → $border_color
@font_inter     → $font_inter
@font-size-base → $font-size-base
@font-size-sm   → $font-size-sm
@font-size-md   → $font-size-md
@line-height-base → $line-height-base
@color-text     → $color-text
@heading-weight → $heading-weight
@btn-bg         → $btn-bg
@btn-color      → $btn-color
@btn-border     → $btn-border
@btn-bg-hover   → $btn-bg-hover
@btn-radius     → $btn-radius
@input-border   → $input-border
@input-bg       → $input-bg
```

---

## Execution Order for Next `/next`

1. **S2-01 Group A** — bulk copy+rename 12 vendor/pure-CSS files
2. **S2-01 Group B** — global-tokens + pikaday: add `@use "vars" as *`, `@var` → `$var`
3. **S2-01 Group C** — table, flex_old, interface, notifier: fix imports only
4. **S2-01 Group D** — skins + app_window_gui: local `@var` → `$var`
5. **S2-01 Group E** — flex.less (LESS loops → SCSS `@for`), swiper/mixins.less (LESS mixins → `@mixin`)
6. **S2-02** — Wire `main.scss`, `_appsite.scss`, `_datatable.scss` with explicit `@use`/`@forward`
7. **S2-03** — Any remaining mixin call-sites not covered above
8. **S2-04** — Remove `lessc.inc.php` from `index.php`
9. **S2-05** — Verify ClassApp, mark done if already complete
