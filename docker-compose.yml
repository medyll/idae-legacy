services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./idae:/var/www/html/idae
      - ./logs:/var/log/apache2
    environment:
      - APACHE_RUN_USER=www-data
      - APACHE_RUN_GROUP=www-data
      - CONF_INC=/var/www/html/idae/web/conf.inc.php
      - DOCKER_ENV=1# Utilise l'alias DNS interne pour l'hôte
      - MONGO_HOST=host.docker.internal
      - MDB_USER=${MDB_USER:-}
      - MDB_PASSWORD=${MDB_PASSWORD:-}
      - MDB_PREFIX=${MDB_PREFIX:-}
      - DEBUG_DB=${DEBUG_DB:-}
      - DEBUG_SESSION=${DEBUG_SESSION:-}
      - MONGO_HOST_PORT=27017
      - SOCKET_PORT=3005
    container_name: idae-legacy
    healthcheck:
      test: ["CMD", "curl", "-f", "-m", "3", "http://localhost/idae/web/check_mongo.php"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s
    restart: unless-stopped
